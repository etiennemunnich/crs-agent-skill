# REQUEST-100001: Runtime exclusion for GraphQL /api/graphql - Rule 942100 (SQL Injection)
# Purpose: Exclude rule 942100 for legitimate GraphQL mutations from Apollo Client at /api/graphql
# Placement: BEFORE CRS include directive (runtime/ctl: approach)
# Context: GraphQL mutations with legitimate strings like "OR 1=1" in mutation parameters

SecRule REQUEST_URI "@streq /api/graphql" \
    "id:100001,\
    phase:2,\
    pass,\
    nolog,\
    ctl:ruleRemoveById=942100"
